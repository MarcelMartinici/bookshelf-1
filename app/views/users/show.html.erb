<p>
  <strong>Name:</strong>
  <%= @user.email %>
</p>
  <strong>Books:</strong>
<ul>
    <% @user.books.each do |book| %>
      <li><%= link_to(book.title, book_path(book)) %></li>
    <% end %>
</ul>
</p>

<%= link_to "Back", users_path %>