<p>
  <strong>Genre:</strong>
  <%= @genre.tag %>
</p>
  <strong>Books listed:</strong>
<ul>
    <% @genre.books.each do |book| %>
      <li><%= link_to(book.title, book_path(book)) %> </li>
    <% end %>
</ul>
</p>

<%= link_to "Back", genres_path %>